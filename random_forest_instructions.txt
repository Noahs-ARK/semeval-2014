first:
install.packages("randomForest")
library(randomForest)
load in the data into a data frame
semdp.rf <- randomForest(V2 ~., data=train, proximity=FALSE)
semdp.pred <- predict(semdp.rf, test)


loading from disk:
fullData = readLines("featsForRF.100")
// will probably have to concatenate datasets here
num <- nrow(fullData)
trainNum <- 1:(num-num/10)
testNum <- (num-num/10+1):num
data <- fullData[,c(-1,-3,-4,-5)]
train <- data[trainNum,]
test <- data[testNum,]

